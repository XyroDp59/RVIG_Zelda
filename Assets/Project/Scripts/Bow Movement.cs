using UnityEngine;
using UnityEngine.InputSystem;

public class BowMovement : MonoBehaviour
{
    [SerializeField] private Transform mainHand;
    [SerializeField] private Transform all;
    [SerializeField] private Transform middleString;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
    }
}
